""" * Crea una función que reciba un número decimal y lo trasforme a Octal
 * y Hexadecimal.
 * - No está permitido usar funciones propias del lenguaje de programación que
 * realicen esas operaciones directamente."""


#----------------------------------------------------------------------------------------------------------
#----------------------------------------------------------------------------------------------------------
#---------------------------------------------------------------------------------------------------------

"""Marco Caro PYandDT\n"""

def octal(num)->int|float:
    return

def hexadecimal(num)->int|float:
    return


def convertidor()->int|float|None:

    try:
        numero= float(input('Ingresa un numero decimal a convertir: '))
        
    except ValueError:
        print('El valor ingresado no es valido como numero decimal')
    
        return '-'*160

    return

def main():

   a= convertidor()
   print(a)

   return

if __name__ == '__main__':
   main()